/** sessionStorage — user OpenAI key for BYOK runs (never sent except as Bearer to same-origin API). */
export const USER_OPENAI_KEY_STORAGE = 'orchestrator_openai_key';

/** sessionStorage — anonymous corpus id for user uploads on the RAG tab (same browser tab). */
export const RAG_COLLECTION_STORAGE = 'orchestrator_rag_collection_id';
